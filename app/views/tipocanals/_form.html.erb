<%= form_for(@tipocanal) do |f| %>
  <% if @tipocanal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipocanal.errors.count, "error") %> prohibited this tipocanal from being saved:</h2>

      <ul>
      <% @tipocanal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
