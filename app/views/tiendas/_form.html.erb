<%= form_for(@tienda) do |f| %>
  <% if @tienda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tienda.errors.count, "error") %> prohibited this tienda from being saved:</h2>

      <ul>
      <% @tienda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :empresa_id %><br>
     <%= f.collection_select :empresa_id, Empresa.order(:nombre), :id, :nombre, include_blank: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
