<%= form_for(@fabricante) do |f| %>
  <% if @fabricante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fabricante.errors.count, "error") %> prohibited this fabricante from being saved:</h2>

      <ul>
      <% @fabricante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
